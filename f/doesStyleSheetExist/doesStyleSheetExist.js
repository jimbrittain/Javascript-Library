"use strict";
/* global window, IMDebugger, __imns, document, console, findStyleSheet */
var doesStyleSheetExist = function(href, name){ return (findStyleSheet(href, name) === false) ? false : true; };

